<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qy.organization.app.infrastructure.persistence.mybatis.mapper.DepartmentMapper">
    <select id="getTopDepartmentId" resultType="java.lang.Long">
        select id from ark_uims_org_department where is_deleted = 0 and parent_id = 0 and organization_id = #{organizationId} order by create_time desc  LIMIT 1
    </select>

</mapper>
