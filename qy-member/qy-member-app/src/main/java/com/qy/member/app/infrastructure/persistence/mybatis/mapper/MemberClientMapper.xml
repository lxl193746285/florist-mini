<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qy.member.app.infrastructure.persistence.mybatis.mapper.MemberClientMapper">
    <select id="findMemberClientList" resultType="com.qy.member.app.application.dto.MemberClientDTO">
        SELECT ammc.*, auc.name client_name FROM ark_mbr_member_client ammc
                                                     LEFT JOIN ark_uims_client auc ON ammc.client_id = auc.client_id
        WHERE auc.is_deleted=0 AND ammc.member_id=#{memberId}
    </select>
    <select id="findMemberClient" resultType="com.qy.member.app.application.dto.MemberClientDTO">
        SELECT ammc.*, auc.name client_name FROM ark_mbr_member_client ammc
                                                     LEFT JOIN ark_uims_client auc ON ammc.client_id = auc.client_id
        WHERE auc.is_deleted=0 AND ammc.member_id=#{memberId} AND auc.client_id=#{clientId} limit 1
    </select>
</mapper>
