<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qy.wf.defDefine.mapper.DefDefineMapper">
    <select id="getCanStartWFDef" resultType="com.qy.wf.defDefine.dto.DefStartDTO">
        SELECT
            wdd.id,
            wdd.NAME,
            wdm.fun_name
        FROM
            wf_def_define wdd
                LEFT JOIN wf_def_map wdm ON wdm.wf_id = wdd.id
        WHERE
            wdd.is_deleted = 0
          AND wdd.`status` = 1
          AND wdd.can_start = 1
          AND wdm.fun_name IS NOT NULL
    </select>
</mapper>
